[build]
builder = "nixpacks"
watchPatterns = ["backend/**"]

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/docs"
restartPolicyType = "on_failure"

[deploy.env]
# Multi-region settings
LIVEKIT_REGION = "${RAILWAY_REGION}"
